server :
  port : 8888

spring:
  mvc:
    static-path-pattern: /static/**
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource :
    driver-class-name : com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306//koreaitdb
    username : koreait
    password : 0000

mybatis:
  mapper-locations:
    - /mappers/*
