<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tut00.repository.AccountRepository">
    <insert id="insertUser" parameterType="com.example.tut00.dto.UserDto">
        insert into user
        values (#{userid}, #{passwd})
    </insert>
</mapper>

